name: Validate Rejected Quote Template 20260829

on:
  push:
    branches: [feature/quote-rejection-reason-20260829]
    paths:
      - 'src/Dhole.Reports.Infrastructure/Generation/TemplateDataBinder.cs'
      - 'src/Dhole.Reports.Persistence/Migrations/20260829141000_AddPricingQuoteRejectionReason.cs'
      - '.github/workflows/validate-quote-rejection-reason-20260829.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: read

jobs:
  validate:
    runs-on: [self-hosted, linux, x64, dhole]
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/quote-rejection-reason-20260829

      - name: Build Reports API image
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          docker build --pull \
            --build-arg GITHUB_ACTOR="$GITHUB_ACTOR" \
            --secret id=github_token,env=GITHUB_TOKEN \
            -f src/Dhole.Reports.Api/Dockerfile \
            -t dhole/reports-quote-rejection-validation .
